build:
  verbosity: minimal

clone_depth: 30

build_script :
- C:\MinGW\bin\mingw-get install libgmpxx
- '%BASH_PATH% -lc "cd $APPVEYOR_BUILD_FOLDER/usr/local ; wget https://netcologne.dl.sourceforge.net/project/mingw/MSYS/Extension/flex/flex-2.5.35-2/flex-2.5.35-2-msys-1.0.13-bin.tar.lzma  && tar --lzma -x -f flex-2.5.35-2-msys-1.0.13-bin.tar.lzma && rm flex-2.5.35-2-msys-1.0.13-bin.tar.lzma && ls -R"'
- '%BASH_PATH% -lc "cd $APPVEYOR_BUILD_FOLDER; ./build_greatspn.sh && mv website/greatspn_linux.tar.gz website/greatspn_windows.tar.gz"'
- DIR

 
environment :
 MSYSTEM: MINGW64 
 BASH_PATH: C:\msys64\usr\bin\bash

 
artifacts:
  - path: website/greatspn_windows.tar.gz
    name: greatspn_windows.tar.gz


      
